DandelionOS Project Progress

Last updated
- 2026-05-08
- Checked against DESIGN.md and current scaffold implementation.

Current focus
- Turn the scaffold into a real DESIGN.md-driven runtime in small safe steps.
- Tighten live behavior and visual fidelity on top of the now-working SDL runtime surface.
- Keep active implementation progress on GitHub branch `v2`, while using `v1` only as a legacy reference for SDL/OpenCV ideas that still fit the current DESIGN.md runtime contracts.

Design alignment summary
- The project now matches the high-level DESIGN.md structure:
  - P1 / P2 / P3 queue separation exists.
  - StartTask / ResetTask / ExitTask exist.
  - CameraTask / MicrophoneTask / BatchParticleExecutionTask now participate in a one-way runtime chain instead of a resident loop.
  - GenerateParticleTask creates P3 particle tasks.
  - BreezeTask exists as a fallback P2 task with fixed-power particle generation.
  - ChangeDandelionTask exists as a P2 task that rebuilds the dandelion cluster.
  - SingleParticleTask is resumable and writes through the ordered shared-state path.
  - RenderData remains task-owned for writes and renderer-owned for reads.
- The main remaining gap is behavioral rather than structural:
  - CameraTask now enforces bridge sample freshness / staleness handling, but still needs tuning against real live camera behavior and mouth-state thresholds
  - MicrophoneTask now supports native C++ capture and JSON/offline bridge input, and consumes each bridge sample once, but breath / voiced / silence classification is still heuristic
  - BatchParticleExecutionTask now records the first world tick, decays `power` in 1-second steps, cleans detached boundary-stopped particles, and finishes when no particle work remains, but still does a simplified world tick compared with full DESIGN.md timing semantics
  - queue fairness around `BatchParticleExecutionTask` plus `P3 RR` is now chain-aware rather than resident-aware, but the exact policy is still simpler than full DESIGN.md RR semantics
  - SDL visualization now reflects the layer split much more clearly, but the camera layer still shows a bridge-status viewport rather than a real embedded camera frame texture
  - `SingleParticleTask` now advances particles using `power * 100 px/s` with `RR_QUANTUM_MS = 16`, but there is still room to refine how live mouth/microphone direction should shape spread and flight aesthetics

Audit against DESIGN.md
- Completed with meaningful behavior:
  - scheduler queue separation and dispatch order
  - interrupt / requeue / resume bookkeeping
  - P1 -> P2/P3 and P2 -> P3 preemption bookkeeping
  - WAITING / SLEEPING thread-state flow
  - StartTask / ResetTask / ExitTask lifecycle
  - GenerateParticleTask / BreezeTask / ChangeDandelionTask / SingleParticleTask runtime execution
  - bridge-ready CameraTask / MicrophoneTask inputs
  - timestamp-based freshness and stale-sample rejection for camera and microphone bridge input
  - offline replay writers for local image and audio samples
  - direct end-to-end validation of `CameraTask -> MicrophoneTask -> Generate/Breeze -> P3`
  - particle boundary-stop and completion-cleanup behavior
  - non-resident `P2` chain staging so replay particle work can complete without manually draining functional tasks
  - one-sample-one-trigger microphone semantics so deterministic replay finishes instead of endlessly re-spawning particle work
  - same-frame preemption victim selection closer to DESIGN.md `lowestPriorityRunningTask` plus `tie -> firstScheduledThread`
  - BatchParticleExecutionTask world-tick responsibilities for decay, particle render cleanup, and remaining-particle reconciliation
  - SingleParticleTask motion is now time-driven instead of fixed-3-cycle completion:
    - spawn direction is primarily outward from the dandelion center
    - distance per dispatch uses `power * 100 px/s` and the 16 ms RR quantum
    - particles now finish on boundary exit instead of an arbitrary cycle cap
  - particle slot ownership tokens and repeated-replay consistency checks
  - unified `queued_particle_tasks` reconciliation from actual P3 queue state plus in-flight particle work, including mixed resident load and partial-frame interruption
- live `main.cpp` entry now launches the real Python MediaPipe camera bridge and uses native C++ microphone capture for manual end-to-end testing
- the Python MediaPipe camera bridge now has its runtime dependencies installed and a local `face_landmarker.task` model available for live use
  - SDL renderer now acts as a real read-only RenderData consumer with layered world feedback, status panels, and interactive control buttons
- Implemented but still simplified relative to DESIGN.md:
  - CameraTask mouth-state behavior
  - MicrophoneTask power and fallback behavior
  - BatchParticleExecutionTask world-tick semantics
  - particle lifecycle cleanup and boundary handling
  - task ordering constraints such as CameraTask-before-MicrophoneTask and stronger queue replacement semantics
- Still missing or only indirectly represented:
  - explicit UI button task plumbing from the DESIGN startup flow
  - device-driven end-to-end live behavior for camera inference inside the runtime loop
  - fuller RR tie-break and lowest-priority-running preemption behavior across multiple active threads
- Branch audit:
  - `v2` should be treated as the active DESIGN.md-aligned integration branch
  - `v1` remains useful only as a reference source for older visualization and OpenCV patterns
  - no `v1` code should be copied forward if it bypasses the current task graph, bridge contracts, or RenderData read/write ownership rules

Completed
- Cleaned the repo and removed the legacy implementation that conflicted with DESIGN.md.
- Rebuilt the project as a compile-ready scaffold.
- Added real startup-system tasks:
  - StartTask
  - ResetTask
  - ExitTask
- Added shared runtime state and a clearer runtime phase model.
- Added queue policy scaffolding for:
  - P1 LIFO-style system dispatch
  - P2 FIFO-style functional dispatch
  - P3 FIFO particle dispatch
- Replaced placeholder CameraTask with a resident task scaffold.
- Added MicrophoneTask as a resident scaffold using the power + render lock path.
- Added GenerateParticleTask as a P2 event task that creates P3 particle task records.
- Added BreezeTask as a real fallback P2 task with fixed power and single-particle fallback spawning.
- Added ChangeDandelionTask as a real P2 task that repositions the dandelion and rebuilds the particle ring.
- Replaced placeholder BatchParticleExecutionTask with a daemon-style resident scaffold.
- Added shared-state mutex scaffolding for particle / power / render writes.
- Added lock-order tracing so ordered writes can be verified in tests.
- Upgraded SingleParticleTask into a resumable P3 skeleton using particle + render writes.
- Preserved a compile-ready visualization placeholder and smoke-test structure.
- Added queue / dispatch bookkeeping for interrupt, requeue, and resume semantics.
- Added P1 -> P2/P3 and P2 -> P3 preemption bookkeeping.
- Added explicit WAITING / SLEEPING thread-state flow for preemption, reset, and mode changes.
- Added resident-task recovery policy for reset and thread-mode changes.
- Added smoke-test coverage for BreezeTask, ChangeDandelionTask, preemption, and resident recovery.
- Replaced CameraTask / MicrophoneTask scripted values with explicit bridge-ready runtime contracts.
- Added a real JSON bridge adapter that reads `camera_bridge_latest.json` and `microphone_bridge_latest.json` into runtime bridge state.
- Added an offline microphone bridge writer for deterministic replay of local sample audio into `microphone_bridge_latest.json`.
- Added an offline camera bridge writer for deterministic replay of local sample images into `camera_bridge_latest.json`.
- Added native C++ microphone capture as a preferred runtime bridge source on Windows when no microphone JSON replay file is present.
- Added timestamp-based freshness checks for camera and microphone bridge consumption.
- Added direct runtime smoke-test coverage for `CameraTask -> MicrophoneTask -> GenerateParticleTask/BreezeTask -> P3`.
- Added `SingleParticleTask` boundary-stop handling and unified finish cleanup for particle render slots.
- Extended replay smoke tests so voiced and fallback chains run through `SingleParticleTask` completion.
- Added a scheduler yield rule that lets P3 run when P2 only contains resident tasks.
- Refined the scheduler fairness rule so:
  - single-thread mixed resident/P3 load alternates between resident P2 and P3
  - multi-thread mixed resident/P3 load reserves one resident P2 slot and lets remaining threads drain P3
  - the same resumable task is not dispatched twice in one frame
- Added microphone sample-consumption bookkeeping so one bridge timestamp only triggers one Generate/Breeze action.
- Refined same-frame preemption semantics so:
  - when a higher-priority task appears, the scheduler marks the lowest-priority running task as the preemption victim
  - if multiple running tasks tie at that lowest priority, the first scheduled thread becomes the victim
  - tests now cover both `P3 + P3 -> injected P2` tie-break and `P2 + P3 -> injected P1` lowest-priority selection
- Upgraded `BatchParticleExecutionTask` so each world tick can:
  - decay `power` toward the 0.1 floor
  - clean detached finished/boundary-stopped particle render records into `particle-cleaned`
  - reconcile `remaining_particles` from attached particle ownership
- Updated `GenerateParticleTask` and `BreezeTask` to spawn only from attached particle slots instead of blindly reusing the first indexes.
- Added per-slot ownership tokens so `SingleParticleTask` only writes the slot generation it was created for.
- Added tests that verify:
  - cleaned slots are skipped by later particle generation
  - repeated replay cycles continue onto new attached slots instead of reusing already-cleaned ones
- Tightened `queued_particle_tasks` bookkeeping so:
  - P3 enqueue / clear / frame-end requeue paths all reconcile against the real queue state
  - in-flight particle work is counted once even during partial-frame interruption and resume
  - repeated replay uses strictly newer microphone timestamps and no longer false-passes on stale UI status
  - mixed resident P2 + P3 smoke tests now assert count stability directly
- Reworked `main.cpp` into a live runtime entry that:
  - starts the Python MediaPipe camera bridge automatically when possible
  - removes stale offline bridge override files before live use
  - runs the scheduler continuously with native C++ microphone capture
  - exposes manual hotkeys for quit, reset, change-dandelion, thread mode, and forced breeze fallback
- Replaced the placeholder `render_sdl.cpp` with a real SDL feedback surface that:
  - opens a resizable live window
  - polls keyboard and close events directly from SDL
  - renders dandelion particles, flying particle work, mouth position, wind direction/power, queue composition, and thread states
  - moves detailed textual status into the window title so `WAITING` can be interpreted alongside `last_task_event`
- Fixed native microphone bridge timestamps to use epoch milliseconds so live freshness checks accept real captured samples.
- Expanded the SDL visualization toward the DESIGN.md layer model by adding:
  - a dedicated camera-layer card with mouth marker and face / forward / open-state feedback
  - a world-layer view that separates background, wind, and particle motion more clearly
  - a side-panel UI that surfaces queue load, device state, thread state, particle counts, and control buttons
  - mouse-clickable SDL buttons for reset, change dandelion, breeze, quit, and thread-mode switching
- Exposed background task/runtime status more directly by:
  - keeping recent runtime notes visible in the SDL side panel
  - showing CameraTask / MicrophoneTask / Generate / Batch / Particle status strings on screen instead of only in the title or shutdown output
  - surfacing camera-bridge auto-start failure as a runtime event note
- Installed the live camera bridge prerequisites and unblocked the MediaPipe task path by:
  - installing `opencv-python` and `mediapipe` into the runtime Python environment
  - downloading `face_landmarker.task` into the project for the tasks API path
  - patching `python_mediapipe_bridge.py` so the model is copied to an ASCII temp path before loading, which avoids Unicode-path failures on Windows
- Hardened live camera access so the bridge now:
  - tries multiple OpenCV camera backends and device indexes instead of relying on a single default open path
  - reports `bridge_connected`, `sample_ready`, backend label, and camera status text back through `camera_bridge_latest.json`
  - distinguishes `camera unavailable`, `camera opened but waiting for frame`, `sample ready, no face`, and `face tracked` states in the runtime UI
- Hardened live launcher camera startup so `main.cpp` now:
  - marks the camera bridge as `starting` before the first packet arrives
  - waits briefly for the first live camera packet after auto-start
  - records a runtime note if the bridge process starts but no packet arrives during the startup window
- Expanded live thread diagnostics so SDL now shows per-thread:
  - current state
  - currently bound task
  - last event
  - last completed task
  - rolling runtime notes for dispatch / resume / preempt / requeue / finish transitions
- Updated `run_live.bat` so the build step still uses MinGW, but the live runtime restores the original `PATH` before launch; this prevents the camera bridge from accidentally preferring the MinGW Python interpreter over the user Python environment.
- Restored DESIGN.md-aligned live defaults and sequencing by:
  - switching the live runtime default back to one-thread mode
  - keeping resident CameraTask, MicrophoneTask, and BatchParticleExecutionTask active even in one-thread mode so functionality is reduced by scheduling, not by removing task types
  - cancelling pending `GenerateParticleTask` / `BreezeTask` work when the camera gate closes so closed-mouth input does not leave stale blow tasks queued behind later work
- Changed runtime notes to print to stdout immediately instead of only being surfaced after shutdown.
- Tightened single-thread `P2` ordering to better match the intended DESIGN.md flow:
  - `CameraTask` runs before `MicrophoneTask`
  - `GenerateParticleTask` / `BreezeTask` run before `BatchParticleExecutionTask` once they are queued
  - smoke tests now lock this single-thread chain in place
- Added input-focus locking for the live sensor chain so one-thread behavior is driven more by task-internal completion conditions than by blind resident alternation:
  - `CameraTask` now keeps single-thread `P2` focus until it internally opens the camera gate or explicitly falls back because the camera is unavailable
  - `MicrophoneTask` then keeps single-thread `P2` focus until it internally consumes the first valid voice/fallback sample for that gate window
  - once a microphone sample has been consumed for the current camera gate, `CameraTask` no longer re-arms microphone focus on every held-open frame
  - tests were updated to validate the chain by waiting for real generate events instead of assuming a fixed tick index
- Tightened particle execution fidelity against the updated DESIGN.md by:
  - changing `BatchParticleExecutionTask` to treat its first run as the initial world tick and to decay `power` in 1000 ms chunks rather than an implicit 3-cycle particle lifetime model
  - changing `SingleParticleTask` to move by time-based distance each RR slice instead of fixed `vx_ / vy_` steps
  - making boundary exit the normal particle completion path for replay and live runtime testing
  - updating unit and runtime smoke tests so they assert boundary-stop / cleaned terminal states instead of assuming the third slice always finishes a particle
- Audited `v1` against the current runtime and confirmed the reuse strategy:
  - keep `v2` as the active branch for all DESIGN.md-aligned runtime work
  - treat `v1` SDL/OpenCV code as reference only
  - preserve the newer bridge-driven live path in `main.cpp`, `render_sdl.cpp`, and `python_mediapipe_bridge.py` instead of restoring direct old-flow camera logic
- Reorganized `DESIGN.md` into a single canonical version focused on camera, microphone, and particle handling:
  - removed conflicting duplicated rules in favor of one forward-only input/particle pipeline
  - made startup device availability latching explicit
  - clarified that if no camera is available, the runtime must start directly from `MicrophoneTask` instead of idling on `CameraTask`
  - clarified that `CameraTask -> MicrophoneTask -> Generate/Breeze -> BatchParticleExecutionTask -> P3` is a one-way consumption chain
  - clarified that `CameraTask` may only be reopened when `P1`, `P2`, and `P3` are all empty
- Reworked the runtime to match that canonical chain more directly:
  - `CameraTask` and `MicrophoneTask` are no longer treated as resident tasks
  - a running task leaves `P1` / `P2` / `P3` when dispatched and only returns on interrupt/resume paths
  - `ResetTask` now reseeds only one input entry task based on latched device availability instead of restoring a resident functional baseline
  - `GenerateParticleTask` / `BreezeTask` now explicitly queue `BatchParticleExecutionTask` as the next chain stage
  - `BatchParticleExecutionTask` now finishes when there is no remaining particle work instead of behaving like a daemon resident
  - idle queue re-entry now follows the current DESIGN.md rule: only when `P1`, `P2`, and `P3` are all empty does the runtime reopen the input chain
- Replaced the old resident-oriented smoke tests with chain-oriented smoke tests that verify:
  - `CameraTask` leaves the queue while running and hands off to `MicrophoneTask`
  - `MicrophoneTask` leaves the queue while running and hands off to `GenerateParticleTask`
  - `GenerateParticleTask` hands off to `BatchParticleExecutionTask` plus `P3`
  - a drained `P1/P2/P3` set reopens the input entry path on the next scheduler tick

In progress
- Stage 5: strengthen runtime behavior quality on top of the completed task graph.
- Shift effort from "missing tasks" to "task fidelity and sequencing".

Local test assets
- Local-only camera sample images are available for bridge and mouth-state testing:
  - `close_0.jpg`
  - `close_1.jpg`
  - `open_0.jpg`
  - `open_1.jpg`
- Local-only microphone sample audio is available for bridge and fallback testing:
  - `huu.m4a`
  - `ahh.m4a`
  - `no voice.m4a`
- These files are for local validation only and are not intended to be shared to GitHub.
- Suggested future use:
  - verify closed-mouth vs open-mouth camera bridge output against paired images
  - compare breath-like audio vs voiced audio vs silence for microphone bridge behavior
  - use silence and fallback-trigger scenarios to validate `MicrophoneTask -> BreezeTask` switching
  - replay `close_0.jpg`, `close_1.jpg`, `open_0.jpg`, and `open_1.jpg` through the offline camera bridge writer for deterministic `CameraTask` testing
  - replay `huu.m4a`, `ahh.m4a`, and `no voice.m4a` through the offline microphone bridge writer for deterministic task testing

Next implementation plan
1. Strengthen MicrophoneTask classification and fallback orchestration using the local replay assets and native capture path.
2. Finish remaining particle lifecycle rules:
   - render record cleanup polish after tasks finish
   - better live spread/direction tuning now that time-driven motion is in place
3. Make task ordering constraints closer to DESIGN.md:
   - CameraTask before MicrophoneTask
   - GenerateParticleTask placement rules
   - fallback switching between MicrophoneTask and BreezeTask
4. Push the SDL renderer from status dashboard toward a fuller product shell:
   - optional embedded camera texture instead of status-only camera viewport
   - clearer on-screen task labels and button semantics from the DESIGN startup flow

Immediate coding priorities
- Move the current working runtime onto branch `v2` as the canonical integration baseline.
- Make ChangeDandelionTask and ResetTask share clearer world-rebuild rules where appropriate.
- Tighten RR fairness details beyond the current resident/P3 sharing rule:
  - clearer frame-level dispatch accounting for mixed resumable workloads
  - closer approximation of continuous-time RR expiry under sequential frame simulation
- Keep expanding tests whenever a scaffolded task becomes behaviorally real.

Next goal
- Goal: complete one full DESIGN.md-aligned "blow cycle" with deterministic replay and closer world-tick fidelity.
- Scope:
  - use offline camera replay to drive mouth-open detection
  - use offline microphone replay to drive voice / breath / silence paths
  - enforce CameraTask -> MicrophoneTask -> GenerateParticleTask or BreezeTask sequencing
  - let BatchParticleExecutionTask and SingleParticleTask carry the world forward with time-driven motion until particles reach boundary stop and cleanup
- Exit criteria:
  - one replayable scenario for mouth-open + breath-like input reaches BreezeTask and visible particle completion
  - one replayable scenario for mouth-open + voiced input reaches GenerateParticleTask and visible particle completion
  - the same scenarios complete while resident functional tasks continue to exist normally
  - stale or silent input does not incorrectly generate particle work

Notes for collaborators
- Keep RenderData write ownership inside tasks only.
- Keep renderer read-only.
- Prefer finishing one lifecycle step completely before adding SDL or device complexity back.
- Treat DESIGN.md as the source of truth when scaffold behavior and older expectations conflict.
- Prefer behavioral correctness in scheduler transitions before visual polish.
